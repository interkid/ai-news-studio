# 次にやることリスト

CLAUDE.mdのマイルストーン順（M4→M5→M6→M7）に対応するタスク一覧。
外部サービスの設定手順は `manual.md` を参照。

---

## M4: TikTok連携（✅ E2E完了 2026-07-07）

daily.yml（収集→台本→TTS→レンダ→R2→LINE承認依頼）→ LINE承認 → publish.yml
（KV検知→TikTok inboxチャンクアップロード→LINE完了通知）まで全段通しで成功
（`[published] script_id=21`）。E2E中に修正した項目:
- 台本の合計文字数(70〜100字)にLLMが収束しない → 実測文字数を添えた最小修正リペアを実装
- ファクト検証の不合格理由がCIログに出ない → WARNINGログ追加
- GitHub Secretsの`FAL_KEY`未登録・`R2_*`誤値(NoSuchBucket)・`CF_API_TOKEN`誤値(401)
  → ローカル`.env`の正値をAPIで同期（scratchpadのsync_secrets.py方式）

### 残タスク
- [x] TikTokアプリの下書きに動画が実際に届いているかの目視確認
      （2026-07-07 新キャラ製script_23で確認済み。品質OKの評価）
- [ ] （必要と判明した場合のみ）TikTok連携の一連の流れを画面録画し、デモ動画として提出

### 現状のTikTok認可情報
- 本番アプリの審査は待たず、**当面Sandboxのclient_key/secretで運用**する方針（GitHub Secrets登録済み）
- Sandboxのtarget usersは自分のアカウントのみ登録。無人自動投稿を本格運用する際は本番アプリの
  審査が別途必要

### 保留中(意図的にOFF)
- [ ] **GitHub Actionsのcronは意図的に無効のまま**: `daily.yml`/`publish.yml`とも
      `workflow_dispatch`のみ有効。`# schedule:`をコメントアウトしてある
      （動画の品質チェックがまだのため、無人自動投稿はONにしない方針。M5の品質確認後に解禁）

---

## M5: デザイン・ネタ・インパクト構成強化（次はここ）

実施順は「1本の動画の見た目品質」への効果が大きい順: **1 → 4 → 2 → 3**
（2026-07-06 見直し。4のプロンプトのビート構成テンプレ化は実装済み）
各項目の完了条件 = `studio render`でサンプル動画を出力し目視で品質確認すること。

**fal.aiの状態（2026-07-07更新）**: $10チャージ済み・疎通確認済みで**稼働中**。
GitHub Secretsにも`FAL_KEY`登録済み（CIのB-roll生成も有効）。

### 1. キャラ/背景素材の刷新（✅ キャラ完了 2026-07-07 / layoutの一部残り）
**キャラ構成の変更（ユーザ決定）**: きつね×うさぎは既存作品との類似感が強いため、
**「キャラメル色の垂れ耳解説うさぎ（丸眼鏡+赤蝶ネクタイ+紺ベスト）×白い聞き役うさぎ（黄スカーフ）」
のうさぎペア**へ変更。内部スピーカーID・ディレクトリ名は`fox`のまま（コード改修を最小化）。
- 画風はピクサー調3DCG（スタイル候補4案からユーザがAを選定）
- Nano Banana Pro編集モードで11状態を生成→rembgで透過→共通bboxで切り出し→コミット済み
- `mouth_closed`は`normal`のコピー（シルエット完全一致）
- `scripts/gen_character_assets.py`(生成) / `scripts/finalize_character_assets.py`(仕上げ)で再現可能
- `assets/layout/bg.png`も同画風（夜空+雲+ボケ光）へ差し替え済み
- **残り**: `logo.png`・`badge_*.png`・`broll_placeholder.png`の刷新（日本語テキストは
  AI生成が苦手なためPillow描画の色・形の改善で対応する方針）

**決定した方式（2026-07-06更新）**: グリッド一括生成ではなく、fal.ai経由の **Nano Banana Pro
（Gemini 3 Pro Image, $0.15/枚）** を第一候補に使う（キャラ同一性維持・表情差分が現行モデルで最も得意。
同じ`FAL_KEY`で呼べる。コスト重視の代替は FLUX.1 Kontext $0.04/枚）。
1. スタイル候補を3〜4案生成（例: ピクサー調3DCG / 毛並みまで描き込んだ厚塗り / リッチなアニメ調）し、
   人間が1案を選定。**キャラとしての質感・リアリティは高くてよい**（禁止は「実写写真と誤認される」
   写実=実在人物・実写風報道写真のみ。キャラと分かる3DCG/リッチイラストは制約に抵触しない）
2. 選定スタイルでベースキャラ（きつね・うさぎ各1枚）を確定
3. 編集モードで「同じキャラの表情違い・口の開閉違い」を差分生成（同一性の維持がグリッド方式より確実）
4. rembg等で背景を透過化 → `manifest.json`の各スロットへ配置
5. 品質不十分ならその時点で外部発注等を検討（予算制約に注意）

**素材仕様（`renderer/layout.py`・`manifest.json`から確定済み）**:
- 透過PNG。配置時に高さ760px(`CHAR_TARGET_H`)へ正規化・足元基準線揃え
- 表情違いでも**シルエット（ポーズ）は同一**にする（表情切替・8fps口パクでガタつくため顔のみ変える）
- 必要枚数: きつね6状態(normal/surprised/smug/worried/mouth_open/mouth_closed)、
  うさぎ5状態(normal/surprised/curious/mouth_open/mouth_closed)
- イラスト調のみ（写実禁止）・商用可ライセンス・`assets/LICENSES.md`に記録

あわせて `assets/layout/` も同じ画風で刷新する: `bg.png`（現状ほぼ無地グラデ）・`logo.png`・
コーナーバッジ4種(`badge_*.png`)・`broll_placeholder.png`。

### 4. フック/展開/オチのテンプレ化（✅ 実装済み 2026-07-06〜07。文字数リペア機構も追加）
`scriptwriter/generate.py`のプロンプトに、exchanges全体のビート構成を明文化する:
- ①つかみ（きつね第一声。hookの言い直しではなく具体の一撃）
- ②リアクション+疑問（うさぎ）
- ③核心・数字・根拠（きつね）
- ④オチ/視聴者への示唆（「あなたの仕事なら〜」等、コメントしたくなる引き）

あわせて`scorer/`に「フック強度・オチの引き」の採点軸を追加し、2案からの選抜基準を
ビート構成と揃える。

### 2. 演出バリエーション追加（4の構成を映像で増幅する位置づけ）
`renderer/effects.py`（現状: zoom_in / pop_in / shake / slide_in / flash / ken_burns）を拡充。
TikTokは冒頭1秒が勝負なのに、現状のフック演出は0.5秒ズームインのみ。優先度の高い順:
- 冒頭インパクト演出（フラッシュ+シェイク+デカ文字ドロップの組み合わせ）
- 感情(surprised/smug等)連動のエフェクト自動選択
- その他バリエーション追加

### 3. ネタストック&ランキング + 人間の事前レビュー（2026-07-07 拡張決定・次はここ）
ユーザ要望: 「日頃から良いネタをストックし、ランキング10を維持したい。キャッチーさ・インパクト・
有用さを追求したい」。旧「ネタ事前レビュー」案を包含する形で以下に拡張。

**方針: 新ツールは増やさず、既存SQLite `topics` テーブルを「ストック台帳」に拡張する。**

実装順（3-a〜3-bを先行、3-c/3-dは後続）:

**3-a. ストック化+3軸採点+鮮度減衰+R2バックアップ（✅ 完了 2026-07-08・CI検証済み）**
- collectorのネタを「使うまで生かすストック」へ変更。既知URLは重複排除、加重合計40点未満は破棄
- LLM採点を relevance 1軸 → **キャッチー0.4/インパクト0.3/有用0.3 の3軸加重合計**に拡張
  （各軸スコアをtopicsに保存。`relevance_score`列は加重合計として継続使用）
- 鮮度減衰: 収集3日後から1日2点減点、30日で自動失効。当日ネタは鮮度減衰込みの
  **ストック全体ランキング**から選ばれる（新着が弱い日は数日前の強いネタが浮上する）
- DB永続化: `studio db backup/restore` CLIを追加し、両ワークフローに組み込み
  （キャッシュmiss時はR2から復元、run終了時にR2へバックアップ。失敗時も`if: always()`で実行）
- CI検証結果: 新着73件→69件ストック・重複排除動作・R2バックアップ成功。
  検証runは動画生成途中で`DailyLimitExceeded`で停止したが、これは同日5回実行による
  コストガードの正常動作（上限はUTC日付=JST朝9時リセット）

**3-b. 毎朝LINEにストック上位10件を配信（✅ 完了 2026-07-08・実配信確認済み）**
- daily.ymlの収集ステージ直後にFlex Messageで配信（タイトル+3軸スコア内訳+ソースURLリンク）
- 配信失敗はWARNINGログのみでパイプラインは止めない。dry-runでは配信しない

**3-c. 手動ストック（LINEでURLを送るだけ）**
- 承認BotへURLを送信 → 既存Cloudflare WorkerがKVへ積む → 次回collector実行時に
  ストックへ取り込み（手動追加はボーナス点を付けて上位に出やすくする）

**3-d. 「これで作る」ボタンで選択生成（3段ワークフロー化・旧レビュー案の完成形）**
- ランキング配信の各ネタに選択ボタンを付け、`video:{id}`と同様に`topic:{id}`キーへKV書き込み
- 「収集」と「生成」を別ジョブ・別トリガーに分割し、2段構成（daily→publish）を
  3段構成（収集→生成→承認/投稿）へ拡張。CIを人間の返信待ちでブロックさせない

---

## M6: 分析ループ（SPEC原文のM5をここに移動）

- [ ] `analytics/` モジュール: TikTok Display APIで投稿48h後・7日後のviews/likes/comments/sharesを取得
- [ ] `weekly.yml`: 週次でLLMに直近4週の成績・却下理由を渡し、「伸びた型/沈んだ型」分析
- [ ] `prompt_insights` テーブルの自動更新（旧行はinactive化、新知見を台本生成プロンプトへ反映）
- [ ] 週次レポートのLINE送信

---

## M7: 運用強化（任意・元M6の残り）

- [ ] コメント返信ドラフト生成（`video.list` + LLM → LINEに返信案送付）
- [ ] 1日2〜3本への増産スイッチ、コーナー別A/B集計ビュー
- [ ] Layer3: 週1〜2本の勝負ネタ限定フック映像（動画生成AI、予備費の範囲内）

---

## 運用メモ: 画像なし1本生成の手順（2026-07-08追加）

daily.ymlのworkflow_dispatchに **`image_daily_limit` 入力**を追加済み（コミット8b515af）。
`0` を指定するとB-roll画像生成を全スキップし、既存の固定背景+キャラ素材のみで合成する
（`imagegen/pipeline.py`の日次上限チェックが0件で即スキップ→プロンプト検査のLLM呼び出しも発生しない）。

- 起動: Actions画面 or API `POST .../actions/workflows/daily.yml/dispatches`
  body `{"ref":"master","inputs":{"image_daily_limit":"0"}}`
- **注意: LLM日次上限の日付境界はUTC**（`db.llm_calls_today()`）。前日にCI検証等で消費していると
  JST朝の実行が開始直後（collector採点）に`DailyLimitExceeded`で落ちる。**JST 9:00にリセット**
  されるのでそれ以降に実行する（2026-07-08朝の試行はこれで失敗。API課金・LINE通知が発生する前に
  停止しており副作用なし）。
- dispatch入力で`DAILY_LLM_CALL_LIMIT`を一時上書きする案は、コストガードを緩める変更のため見送った。

---

## 現在地点（2026-07-07更新）

**M4完了**: daily.yml→LINE承認→publish.yml→TikTokドラフト送信までE2Eで成功。
GitHub Actionsのcronは品質チェック待ちで引き続き意図的に保留中(手動実行のみ有効)。

M5進行中。完了済み: 1のキャラ刷新（うさぎペア化・品質ユーザ確認済み）、
4のビート構成テンプレ化+文字数リペア機構、3-a（ネタストック化+3軸採点+R2バックアップ）、
3-b（LINEトップ10配信）。

残りの実施順: **2（演出バリエーション）→ 3-c（手動ストック）→ 3-d（選択生成ボタン）**。
1のlayout残り（ロゴ・バッジ）は隙間で実施。
